{
  "name": "shimmeraiser-layer",
  "version": "0.1.0",
  "description": "Sample extension description.",
  "scripts": {
    "start": "zem start",
    "build": "zem build",
    "clean": "zem clean",
    "exec": "zem exec",
    "publish": "zem publish"
  },
  "zeplin": {
    "displayName": "shimmeraiser-layer",
    "projectTypes": [
      "web"
    ]
  },
  "dependencies": {
    "zem": "^0.3.5"
  }
}
